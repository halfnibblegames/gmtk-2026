namespace HalfNibbleGame.Planning;

// Using an enum so we can make player actions easily available in the Godot Editor
public enum PlayerAction {
  None = 0,
  MoveLeft,
  MoveRight,
  MoveUp,
  MoveDown,
}
